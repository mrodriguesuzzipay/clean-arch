name: flutter_clean_arch
description: Demonstration of Clean Arch in Flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  dio: ^4.0.6
  flutter_mobx: ^2.0.6+1
  mobx: ^2.0.7+2
  build_runner: ^2.1.11
  get_it: ^7.2.0
  mobx_codegen: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true